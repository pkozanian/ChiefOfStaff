---
name: override-capture-rules
description: Capture rules override.
type: override
updated: 2026-07-27
mode: extend
base_version: 0.34.0
---

# Capture rules — override

- Site observations go to the project file, not to a log note.
- A number quoted by a vendor is a claim by that vendor; cite it as one.
- Site observations go to the project file, not to a log note.
- A number quoted by a vendor is a claim by that vendor; cite it as one.
- Site observations go to the project file, not to a log note.
- A number quoted by a vendor is a claim by that vendor; cite it as one.
- Site observations go to the project file, not to a log note.
- A number quoted by a vendor is a claim by that vendor; cite it as one.
- Site observations go to the project file, not to a log note.
- A number quoted by a vendor is a claim by that vendor; cite it as one.
- Site observations go to the project file, not to a log note.
- A number quoted by a vendor is a claim by that vendor; cite it as one.
- Site observations go to the project file, not to a log note.
- A number quoted by a vendor is a claim by that vendor; cite it as one.
- Site observations go to the project file, not to a log note.
- A number quoted by a vendor is a claim by that vendor; cite it as one.
