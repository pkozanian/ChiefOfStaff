---
description: Create a new virtual team member — a specialist agent with a domain method, a job-typical persona, and evidence-backed starter memory.
argument-hint: "[optional: the specialization, e.g. 'real-time audio']"
---

Onboard a new virtual team member now, following `brain/playbooks/team.md` (schema:
`brain/schemas/team-member.md`).

- Run the focused intake: mandate, specialization, scope & boundaries, definition of success, the
  **`## Method`** (draft 5–8 domain-specific ordered checks yourself, ground it with one question, and
  confirm it alongside the **persona** and the drafted **`## Memory scope`** — what this specialist keeps
  and at what grain — in a single beat), optional `## Key context` pointers (start-here shared-brain
  links, not a limit), tools. If `$ARGUMENTS` (or the request) names a specialization, start
  there.
- Build the specialist folder `memory/team/<slug>/`: write `charter.md` (with any `## Key context`
  pointer list), and create its `memory/index.md` and
  `ledger.md`. The team exists iff `memory/index.md` links the roster `memory/team/index.md` — there
  is no other membership signal.
- **Creating the first specialist is atomic:** create `memory/team/index.md` (the roster, `type: log`)
  **and** add a generic `## Always load` pointer in `memory/index.md` —
  `[Virtual team](team/index.md) — your specialists; route via its Active roster.` (never echo
  the specialist's specialization in that pointer) — **in the same turn** as the specialist folder. For a
  **later** specialist, the roster already exists — just add the new `## Active` entry; don't recreate it.
- **Add the specialist's `## Active` roster entry** in `memory/team/index.md` — name, charter link, and a
  one-line routing hook.
- Self-audit before finishing (`brain/playbooks/team.md`): the charter exists with no leftover `AGENTS.md`/`CLAUDE.md` beside it; any
  `## Key context` pointer links resolve and no specialist file links the registry; the specialist is linked
  under `## Active` in `memory/team/index.md` and reachable by following links from `memory/index.md` —
  never by scanning `memory/team/`; no confidential real name in the specialist's files. Fix before
  finishing, then tell me what you created and how to reach the specialist.
