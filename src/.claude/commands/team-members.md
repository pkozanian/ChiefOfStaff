---
description: List the principal's virtual team members and how to reach each.
---

Show my virtual team now, following `brain/playbooks/team.md`.

- The team exists iff `memory/index.md` links the roster `memory/team/index.md`. List from the
  roster's `## Active` section — **never** by scanning/globbing `memory/team/*/charter.md` — then
  open each linked charter for specialization and status. Give how to reach each (talk
  directly, or ask you to delegate). Mention `## Inactive` specialists only if asked.
- If `memory/index.md` doesn't link a roster, or the roster's `## Active` section is empty, say I
  don't have a team yet and that I can create one with `/team-member-onboard` (or just by asking).
- If `memory/team-suggestions.md` has a `## Pending` entry, mention it in one line — with or without
  a team — so a specialist you've suggested isn't invisible here.
