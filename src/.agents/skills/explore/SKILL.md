---
name: explore
description: Surface non-obvious connections across the Chief of Staff's memory (people, projects, decisions).
argument-hint: "[optional: a person, project, or theme to center on]"
---

Run the explore workflow now, following `brain/playbooks/explore.md`.

- If `$ARGUMENTS` (or the request) names a person, project, or theme, center the exploration there;
  otherwise scan all of `memory/`.
- Read `memory/index.md` first, then the memory files; find entities/topics that recur across multiple files
  but aren't directly linked.
- Surface the few most interesting connections (bottom line first): the link, the insight, and what
  would confirm it. **Ground every connection in stored memory — never invent one.**
- Offer to add cross-reference links or a new memory file on my OK; if a connection reveals a gap, flag it.
- If a domain recurs across my memory with no team member covering it, suggest a specialist — offer
  it in this turn and record it as pending; a yes stands it up, and a no is permanent.

If `memory/` is empty or missing, say so and do nothing.
